import { createContext } from "react";

export const ThemeContext = createContext<null>(null);
export const TaskContext = createContext<null>(null);
export const FilterContext = createContext<null>(null);